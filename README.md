# GovCMS Developer Documentation

A PoC port of the GovCMS GitHub Wiki to Zensical, the successor to Material For MkDocs.

## Local build instructions

First install Zensical locally:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install zensical
```

Then build the site:

```
zensical serve
```
