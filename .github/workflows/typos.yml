name: Typo Check

on:
  pull_request:
    paths:
      - 'docs/**'

jobs:
  typos:
    name: Check for typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@master
        with:
          files: ./docs
